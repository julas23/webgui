#!/usr/bin/expect

set timeout 60

spawn ssh root@192.168.1.17 "netstat -putan |grep ESTABELECIDA |grep oracle |grep [lindex $argv 0]"

expect "yes/no" {
    send "yes\r"
    expect "*?assword" { send "C0v48r4kT#\r" }
    } "*?assword" { send "C0v48r4kT#\r" }
expect "# " { send "exit\r" }

interact
